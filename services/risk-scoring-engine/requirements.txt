fastapi
uvicorn[standard]
pydantic
psycopg2-binary>=2.9.9
redis>=5.0.0

# fusion model over Score A + Score B + Score C
scikit-learn
lightgbm
numpy
requests>=2.31

# blockchain anchoring of finalized decisions (chain.py) — any EVM chain: Anvil / Polygon / Besu
web3>=7,<8

# testing
pytest>=8.0.0
httpx>=0.27.0

# Prometheus metrics (/metrics endpoint)
prometheus-fastapi-instrumentator>=7.0.0
