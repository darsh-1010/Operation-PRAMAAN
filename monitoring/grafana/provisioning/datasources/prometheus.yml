# Auto-provisions Prometheus as a Grafana datasource on startup — no manual "Add data
# source" click-through needed. See docker-compose.yml's grafana service.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
